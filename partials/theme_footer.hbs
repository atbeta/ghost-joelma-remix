    <footer class="site-footer clearfix">
        <div class="container">
            <div class="row">
                <div class="col-sm-6 col-lg-3">
                    <h3>{{t "About"}}</h3>
                    <p>本站主要内容将专注于 macOS 使用、软件分享、前端开发学习以及服务器维护等，偶尔也会发一些无关内容，欢迎各位交流！</p>
                </div>
                {{#if @labs.publicAPI}}
                    {{#get "tags" limit="all" include="count.posts"}}
                        {{#if tags}}
                            <div class="col-sm-6 col-lg-3">
                                <h3>{{t "Tags"}}</h3>
                                <ul class="tags">
                                    {{#foreach tags}}
                                        {{#if count.posts}}
                                            <li>
                                                <a href="{{url}}">{{name}}</a>
                                            </li>
                                        {{/if}}
                                    {{/foreach}}
                                </ul>
                            </div>
                        {{/if}}
                    {{/get}}
                {{/if}}
                {{#if @labs.publicAPI}}
                <div class="col-sm-6 col-lg-3">
                    <h3>{{t "Latest posts"}}</h3>
                    <ul class="latest-posts">
                    {{#get "posts" limit="2"}}
                        {{#foreach posts}}
                        <li>
                            <h5><a href="{{url}}" title="{{title}}">{{title}}</a></h5>
                            <time class="post-date" datetime="{{date format="YYYY-MM-DD"}}"><i class="fas fa-calendar-alt"></i> {{date format="DD MMM YYYY"}}</time>
                        </li>
                        {{/foreach}}
                    {{/get}}
                    </ul>
                </div>
                {{/if}}
                {{#if @labs.subscribers}}
                <div class="col-sm-6 col-lg-3">
                    <h3>{{t "Newsletter"}}</h3>
                    <div class="subscribe-container">
                        <p>{{t "Subscribe to our newsletter to receive our latest news."}}</p>
                        {{subscribe_form
                            form_class="gh-signin"
                            input_class="gh-input"
                            button_class="fas fa-angle-right"
                            placeholder=(t "Your email address")
                        }}
                    </div>
                </div>
                {{/if}}
            </div>
        </div>
        <div class="container-fluid copyright">
            <div class="row">
                <div class="col-md-12 text-center">
                    <p class="copyright">{{t "Copyright"}} &copy; {{date format="YYYY"}}, <a href="{{@blog.url}}">{{@blog.title}}</a>. {{t "All Right Reserved"}}.</p>
                </div>
            </div>
        </div>
    </footer>
